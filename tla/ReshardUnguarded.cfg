\* Reshard config-swap model, UNGUARDED variant — the NEGATIVE TEST.
\* GuardedSwap = FALSE removes the is_clean gate from ConfigSwap; TLC is
\* EXPECTED TO FAIL here with a NoStaleEndpointWrite counterexample (the
\* minimal 5-step race documented in Reshard.tla's header:
\* SrcAdvance, Read, FlushStart, ConfigSwap, FlushCommit). A green run of
\* this cfg would mean the model lost the race — i.e. the guarded proof in
\* Reshard.cfg had gone vacuous. Keep this cfg failing.
SPECIFICATION Spec

CONSTANTS
    MaxSnaps = 4
    GuardedSwap = FALSE

CHECK_DEADLOCK FALSE

INVARIANT NoStaleEndpointWrite
INVARIANT PositionBound
INVARIANT FlushStateOK
