apiVersion: v1
kind: Service
metadata:
  name: viaduck
spec:
  clusterIP: None  # headless — for Prometheus scraping
  selector:
    app: viaduck
  ports:
  - name: metrics
    port: 8000
    targetPort: 8000
